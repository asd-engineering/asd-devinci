stages:
  - lint
  - test
  - release

shellcheck:
  stage: lint
  image: koalaman/shellcheck-alpine:stable
  script:
    - shellcheck --severity=warning scripts/*.sh scripts/lib/*.sh

validate-component:
  stage: lint
  image: python:3.11-slim
  script:
    - pip install -q pyyaml
    - python3 -c "import yaml, sys; list(yaml.safe_load_all(open('templates/dev-environment.yml')))"

validate-gitlab-ci:
  stage: lint
  image: python:3.11-slim
  script:
    - pip install -q pyyaml
    - python3 -c "import yaml, sys; yaml.safe_load(open('.gitlab-ci.yml'))"

# On-demand DevInCi session — same as GitHub devinci.yml but local scripts
# Trigger manually from GitLab UI: Run pipeline → set variables to override defaults
devinci:
  stage: test
  image: ubuntu:22.04
  rules:
    - if: $CI_PIPELINE_SOURCE == "web"
      when: manual
  variables:
    ASD_VERSION: latest
    ASD_ENDPOINT: https://api.asd.host
    INTERFACE_TYPE: ttyd
    SESSION_USERNAME: asd
    SESSION_PASSWORD: devinci
    SESSION_SHELL: bash
    TUNNEL_NAME: devinci-$CI_PIPELINE_ID
    REGION: ''
    TTL_MINUTES: '0'
    DIRECT_MODE: 'false'
  before_script:
    - apt-get update -qq && apt-get install -y -qq curl jq git openssl
    - export SCRIPTS_DIR=$CI_PROJECT_DIR/scripts
  script:
    - source "${SCRIPTS_DIR}/setup.sh"
    - source "${SCRIPTS_DIR}/provision.sh"
    - source "${SCRIPTS_DIR}/start-interface.sh"
    - source "${SCRIPTS_DIR}/connect.sh"
  environment:
    name: devinci/$CI_PIPELINE_ID
    url: "${TUNNEL_URL}"
  artifacts:
    reports:
      dotenv: devinci.env
    paths:
      - workspace/tunnel-url.txt
      - workspace/summary.md

# Publish to CI/CD Catalog on tagged releases
create-release:
  stage: release
  image: registry.gitlab.com/gitlab-org/release-cli:latest
  rules:
    - if: $CI_COMMIT_TAG =~ /^\d+\.\d+\.\d+$/
  script:
    - echo "Creating release ${CI_COMMIT_TAG}"
  release:
    tag_name: $CI_COMMIT_TAG
    description: "Release ${CI_COMMIT_TAG} of ASD DevInCi component"
